on:
  push:

jobs:
  security-test:
    runs-on: ubuntu-latest
    steps:
    -  run: |
         echo "Security"
         echo "env: ${{ secrets.GITHUB_TOKEN }}"
         echo "direct: $GITHUB_TOKEN"

         echo "env: ${{ env.ACTIONS_RUNTIME_TOKEN }}"
         echo "direct: $ACTIONS_RUNTIME_TOKEN"
    - uses: hmarr/debug-action@v2